<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bookmanager">

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.example.App"
        android:allowBackup="true"
        android:icon="@mipmap/ic"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.GuideActivity"
            android:theme="@style/AppThemeFull">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ui.MainActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.UserHomeActivity"
            android:launchMode="singleTask" />
        <activity
            android:name=".ui.AdminHomeActivity"
            android:launchMode="singleTask" />
        <activity android:name=".ui.AdminLoginActivity" />
        <activity android:name=".ui.UserLoginActivity" />
        <activity android:name=".ui.UserManagerActivity" />
        <activity android:name=".ui.BooksActivity" />
        <activity android:name=".ui.AddBookActivity" />
        <activity android:name=".ui.ActivitiesActivity" />
        <activity android:name=".ui.AddActivityActivity" />
        <activity android:name=".ui.ActivityDetailActivity" />
        <activity android:name=".ui.CommentsActivity" />
        <activity android:name=".ui.QRCodeImageActivity" />
        <activity android:name=".ui.BooksStatisticalActivity" />
        <activity android:name=".ui.MessagesStatisticalActivity" />
        <activity android:name=".ui.AgreeBorrowActivity" />

        <meta-data
            android:name="android.max_aspect"
            android:value="2.1" />

    </application>

</manifest>